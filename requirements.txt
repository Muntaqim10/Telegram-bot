# ───── RallyHunter_v26 — requirements.txt ─────
# Using flexible pins to pick pre-built wheels on Python 3.14 (no MSVC/Rust needed)

fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.30.0,<1.0
pydantic>=2.9.0,<3.0          # 2.9+ ships cp314 pydantic-core wheels

# Async Redis
redis[hiredis]>=5.0.0,<6.0

# Async HTTP client
aiohttp>=3.9.5,<4.0

# Quantitative
numpy>=2.0.0,<3.0
pandas>=2.2.0,<3.0
# LLM inference
openai>=1.0.0,<2.0


# ENV management
python-dotenv>=1.0.0,<2.0

# Timezone support on Windows
tzdata

# XGBoost Sentinel (ML validation layer)
xgboost>=2.1.0,<3.0

scikit-learn>=1.5.0,<2.0
pyarrow>=17.0.0,<19.0
pytest>=8.0.0,<9.0

# Development Tools
ruff>=0.4.0